<%= form_for [@challenge, @challenge_item] do |form| %>
    <div id="error_explanation">
      <h2><%= pluralize(@challenge_item.errors.count, "error") %> prohibited this challenge item from being saved:</h2>

      <ul>
      <% @challenge_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>


  <%= form.label :content %>
  <%= form.text_field :content %>

  <%= form.submit "Save" %>
<% end %>
